version: '3'

services:
    web:     
        image: nginx:latest
        container_name: my-nginx
        ports:
            - "80:80"
        volumes:
          - ./app:/app
          - ./nginx-conf:/etc/nginx/conf.d/